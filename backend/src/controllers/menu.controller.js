import asyncHancHandler from "../utils/asyncHandler.js";

const addMenu = asyncHancHandler(async (req, res) => {});

const menuList = asyncHancHandler(async (req, res) => {});

export { addMenu, menuList };
